import React, { useContext } from 'react'

import Players from '../search'
import { store } from '../store'

function Home() {
  // const { state } = useContext(store)
  // return <div>{state.loggedIn ? <Players /> : 'Unauthenticated'}</div>
  return <div>Buckets</div>
}

export default Home
